{% comment %}
  Product Card Button
  - "Add to Cart" for available products
  - "Notify Me" for sold out products
  - Custom hover styles
{% endcomment %}

{% liquid
  assign product = closest.product
  assign variant = product.selected_or_first_available_variant
  assign sold_out = false
  unless variant.available
    assign sold_out = true
  endunless
%}

<div class="product-card-button-container" {{ block.shopify_attributes }} style="display: flex; justify-content: center;">
  {% if sold_out %}
    <button
      type="button"
      class="product-card-btn product-card-btn--sold-out"
      disabled
    >
      NOTIFY ME
    </button>
  {% else %}
    <form method="post" action="{{ routes.cart_add_url }}" class="product-card-btn-form" style="width: 100%; display: flex; justify-content: center;">
      <input type="hidden" name="id" value="{{ variant.id }}">
      <button
        type="submit"
        class="product-card-btn product-card-btn--add-to-cart"
      >
        ADD TO CART
      </button>
    </form>
  {% endif %}
</div>

<style>
  .product-card-button-container {
    padding-top: 10px;
    width: 100%;
  }
  
  .product-card-btn {
    width: fit-content;
    min-width: 160px;
    margin: 0 auto;
    padding: 10px 20px;
    border: 1px solid transparent;
    border-radius: 50px; /* Pill shape as requested elsewhere */
    font-weight: bold;
    font-size: 16px; /* Increased +2px from 14px */
    text-transform: uppercase;
    cursor: pointer;
    transition: all 0.3s ease;
    text-align: center;
    display: block;
    line-height: 1.5;
  }

  /* Sold Out - Red/Orange Theme */
  .product-card-btn--sold-out {
    background-color: #D95D39; /* Orange-Red from image/request */
    color: #ffffff;
    border-color: #D95D39;
    cursor: not-allowed;
    opacity: 1; /* Make sure it looks solid */
  }

  /* Add to Cart - Green Theme */
  .product-card-btn--add-to-cart {
    background-color: #A3CA6D;
    color: #ffffff;
    border-color: #A3CA6D;
  }

  /* Hover Effects */
  .product-card-btn--sold-out:hover {
    background-color: #ffffff;
    color: #D95D39;
    border-color: #D95D39;
  }

  .product-card-btn--add-to-cart:hover {
    background-color: #ffffff;
    color: #A3CA6D;
    border-color: #A3CA6D;
  }
</style>

{% schema %}
{
  "name": "Product Card Button",
  "settings": [],
  "presets": [
    {
      "name": "Product Card Button"
    }
  ]
}
{% endschema %}
